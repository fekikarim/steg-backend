package tn.steg.backend.workflow.domain.model;

public enum WorkflowActionType {
    APPROVAL,
    VALIDATION,
    SUBMISSION,
    REJECTION,
    CORRECTION_REQUEST,
    CANCELLATION,
    COMPLETION
}
