/**
 * Outbound ports for notification delivery channels (Phase A10).
 * Adapters live in infrastructure; fakes in tests — callers never change.
 */
package tn.steg.backend.notification.application.port.out;
